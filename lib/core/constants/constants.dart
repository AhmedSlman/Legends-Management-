import '../network/local/cache_helper.dart';

dynamic myToken = CacheHelper.getData(key: 'token');

dynamic employeeName = CacheHelper.getData(key: 'employee_name');

dynamic employeeCode = 'KQ1rv9rCwf';
dynamic employeeRole = CacheHelper.getData(key: 'employee_role');
dynamic adminRole = CacheHelper.getData(key: 'admin_role');
